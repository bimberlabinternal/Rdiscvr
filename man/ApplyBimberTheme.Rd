% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotFunctions.R
\name{ApplyBimberTheme}
\alias{ApplyBimberTheme}
\title{ApplyBimberTheme}
\usage{
ApplyBimberTheme(
  plot = NULL,
  standardPlot = TRUE,
  legendPosition = "right",
  face = "bold",
  titleJust = "center",
  baseSize = 12,
  axisTextsize = 16,
  forceLegendAlpha = TRUE,
  adjustLegendKeySize = TRUE,
  useArrows = FALSE,
  arrowLength = 0.3,
  arrowLinewidth = 1.5,
  arrowHeadLenMm = 2,
  arrowHeadAngle = 50,
  xLabel = "X_Axis",
  yLabel = "Y_Axis",
  arrowLabelOffset = 0.02,
  arrowYlabelAngle = 90
)
}
\arguments{
\item{plot}{a ggplot object}

\item{standardPlot}{boolean controls theming defaults dependant on a plot is a UMAP, DonutPlot, or other non-Standard plot}

\item{legendPosition}{character, default `"right"`. Legend position passed to
`theme(legend.position = ...)`. Supported values: `"none"`, `"left"`, `"right"`,
`"bottom"`, `"top"`.}

\item{face}{character, default `"bold"`. Font face for plot titles, subtitles,
and facet strips (e.g., `"plain"`, `"bold"`, `"italic"`, `"bold.italic"`).}

\item{titleJust}{character, default `"center"`. Horizontal justification of
title/subtitle. One of `"left"`, `"center"`, `"right"`.}

\item{baseSize}{numeric, default `12`. Base font size fed to the base theme.}

\item{axisTextsize}{numeric, default `16`. Font size for axis titles and tick
labels when `standardPlot = TRUE`. Also used to scale arrow-label text.}

\item{forceLegendAlpha}{logical, default `TRUE`. If `TRUE`, legend guide
overrides mapped alpha to `1` so legend keys are fully opaque.}

\item{adjustLegendKeySize}{logical, default `TRUE`. If `TRUE`, increases
legend key width/height for readability.}

\item{useArrows}{logical, default `FALSE`. If `TRUE`, removes default axis
lines and draws perpendicular x/y arrows at the lower-left
of the panel with customizable head/shaft and centered labels.}

\item{arrowLength}{numeric in (0,1), default `0.3`. Arrow length as
a fraction of the respective axis range (x and y independently).}

\item{arrowLinewidth}{numeric, default `1.5`. Line width of the arrow shafts.}

\item{arrowHeadLenMm}{numeric, default `2`. Arrowhead length in millimeters.}

\item{arrowHeadAngle}{numeric, default `50`. Arrowhead angle (larger =
“bigger” head).}

\item{xLabel}{character, default `"X_Axis"`. Text shown at the midpoint of
the x-axis arrow.}

\item{yLabel}{character, default `"Y_Axis"`. Text shown at the midpoint of
the y-axis arrow.}

\item{arrowLabelOffset}{numeric, default `0.02`. Offset of the label text
from the arrow shafts, expressed as a fraction of the corresponding axis
range (x-label offset is vertical; y-label offset is horizontal).}

\item{arrowYlabelAngle}{numeric, default `90`. Rotation for the y-axis arrow
label (set to `90` for vertical).}
}
\value{
ggplot object
}
\description{
Wrapper for Rdiscvr plotting themes that formats ggplot objects using a standardized theme.
}
